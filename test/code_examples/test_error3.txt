if (limite >= 100) {
    base = 8 + b * 6 - d / e;
    if (base == 110) {
        estado = !activo;
    }
}

if (activo && 1) {
    a = b + * 6;
    2 = @a + 4@ *@ 2@@@@@@@
    if(2)
        pepe;
        //juan;
    else{
      // some code
    }
}

if (base > 5) {
    8 = limite;
} else {
    base = -limite;
}

